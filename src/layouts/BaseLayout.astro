---
const {
	title = "Ronnie Wang",
	description = "Ronnie Wang is a software engineer passionate about AI and penguins."
} = Astro.props;

const pathname = Astro.url.pathname;
const basePath = import.meta.env.BASE_URL.endsWith("/")
	? import.meta.env.BASE_URL
	: `${import.meta.env.BASE_URL}/`;
const hrefWithBase = (path: string) => `${basePath}${path.replace(/^\/+/, "")}`;
const currentPath = pathname.startsWith(basePath)
	? `/${pathname.slice(basePath.length)}`
	: pathname;
const penguinImage = hrefWithBase("penguin_profile.png");
const socialImage = new URL(penguinImage, Astro.site ?? Astro.url).toString();
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:type" content="website" />
		<meta property="og:image" content={socialImage} />
		<meta name="twitter:card" content="summary" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={socialImage} />
		<link rel="icon" type="image/png" href={penguinImage} />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600;9..144,700;9..144,800;9..144,900&family=IBM+Plex+Mono:wght@400;500;600&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap"
			rel="stylesheet"
		/>
		<title>{title}</title>
		<style is:global>
			:root {
				--ink: #f5f7ec;
				--ink-muted: #b8c6c6;
				--ink-soft: #84989a;
				--night: #071019;
				--night-deep: #02060a;
				--night-veil: rgba(7, 16, 25, 0.74);
				--paper: #f4efe0;
				--paper-deep: #e7dfcc;
				--paper-ink: #182026;
				--paper-muted: #50605d;
				--line: rgba(191, 246, 228, 0.2);
				--line-strong: rgba(201, 255, 238, 0.42);
				--green: #70ff9e;
				--cyan: #61f2ff;
				--blue: #5489ff;
				--red: #ff5d6f;
				--violet: #b572ff;
				--gold: #ffe7a8;
				--shadow: 0 28px 90px rgba(0, 0, 0, 0.36);
				--page: 1120px;
				--radius: 8px;
				color: var(--ink);
				background: var(--night-deep);
				font-family: "IBM Plex Sans", "Helvetica Neue", sans-serif;
			}

			* {
				box-sizing: border-box;
			}

			html {
				background: var(--night-deep);
				scroll-behavior: smooth;
			}

			body {
				position: relative;
				margin: 0;
				min-height: 100vh;
				overflow-x: hidden;
				/* deep polar night with ambient aurora skyglow bleeding from the top */
				background:
					radial-gradient(135% 70% at 50% -12%, rgba(112, 255, 158, 0.12), transparent 56%),
					radial-gradient(90% 60% at 82% -4%, rgba(181, 114, 255, 0.11), transparent 60%),
					radial-gradient(80% 55% at 18% 2%, rgba(97, 242, 255, 0.09), transparent 60%),
					linear-gradient(180deg, rgba(2, 6, 10, 0.22), var(--night-deep) 70%),
					linear-gradient(158deg, #04111c 0%, #061826 42%, #0d0a1e 100%);
				color: var(--ink);
			}

			/* aurora curtains: near-vertical rays that ripple and drift sideways */
			body::before {
				position: fixed;
				inset: -16vh -22vw auto -22vw;
				z-index: 0;
				height: 88vh;
				background:
					repeating-linear-gradient(
						95deg,
						transparent 0 2.4rem,
						rgba(112, 255, 158, 0.17) 3.3rem,
						rgba(97, 242, 255, 0.21) 4.6rem,
						transparent 6rem 8.5rem
					),
					repeating-linear-gradient(
						87deg,
						transparent 0 3.2rem,
						rgba(181, 114, 255, 0.14) 4.8rem,
						rgba(84, 137, 255, 0.16) 6.4rem,
						transparent 8rem 12.5rem
					);
				background-size: 220% 100%, 250% 100%;
				background-position: 0% 0%, 100% 0%;
				content: "";
				filter: blur(34px) saturate(165%);
				mask-image: linear-gradient(
					to bottom,
					rgba(0, 0, 0, 0.96) 0%,
					rgba(0, 0, 0, 0.55) 42%,
					transparent 82%
				);
				-webkit-mask-image: linear-gradient(
					to bottom,
					rgba(0, 0, 0, 0.96) 0%,
					rgba(0, 0, 0, 0.55) 42%,
					transparent 82%
				);
				opacity: 0.95;
				pointer-events: none;
				transform: skewX(-8deg);
				animation: aurora-drift 28s ease-in-out infinite alternate;
			}

			/* starfield: scattered points, densest near the top, gentle twinkle */
			body::after {
				position: fixed;
				inset: 0;
				z-index: 0;
				background-image:
					radial-gradient(1.5px 1.5px at 12% 16%, rgba(255, 255, 255, 0.9), transparent 60%),
					radial-gradient(1.5px 1.5px at 27% 40%, rgba(214, 247, 255, 0.8), transparent 60%),
					radial-gradient(1px 1px at 41% 11%, rgba(255, 255, 255, 0.75), transparent 60%),
					radial-gradient(1.5px 1.5px at 56% 26%, rgba(255, 255, 255, 0.85), transparent 60%),
					radial-gradient(1px 1px at 67% 14%, rgba(214, 247, 255, 0.7), transparent 60%),
					radial-gradient(1.5px 1.5px at 81% 34%, rgba(255, 255, 255, 0.8), transparent 60%),
					radial-gradient(1px 1px at 91% 20%, rgba(255, 255, 255, 0.7), transparent 60%),
					radial-gradient(1px 1px at 19% 58%, rgba(255, 255, 255, 0.58), transparent 60%),
					radial-gradient(1px 1px at 48% 52%, rgba(255, 255, 255, 0.5), transparent 60%),
					radial-gradient(1.5px 1.5px at 73% 60%, rgba(255, 255, 255, 0.6), transparent 60%),
					radial-gradient(1px 1px at 34% 70%, rgba(255, 255, 255, 0.42), transparent 60%),
					radial-gradient(1px 1px at 88% 64%, rgba(255, 255, 255, 0.4), transparent 60%),
					radial-gradient(1px 1px at 6% 36%, rgba(255, 255, 255, 0.55), transparent 60%),
					radial-gradient(1px 1px at 62% 6%, rgba(255, 255, 255, 0.65), transparent 60%);
				content: "";
				mask-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.95), rgba(0, 0, 0, 0.45) 55%, transparent 88%);
				-webkit-mask-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.95), rgba(0, 0, 0, 0.45) 55%, transparent 88%);
				opacity: 0.85;
				pointer-events: none;
				animation: twinkle 7s ease-in-out infinite alternate;
			}

			a {
				color: inherit;
				text-decoration: none;
			}

			a:focus-visible {
				outline: 3px solid var(--cyan);
				outline-offset: 4px;
			}

			/* no bar: nav floats on the sky. Children stay clickable, the strip itself doesn't block the hero. */
			.site-nav {
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				z-index: 10;
				display: flex;
				align-items: center;
				justify-content: space-between;
				gap: 1rem;
				padding: 1.7rem max(1.25rem, calc((100vw - var(--page)) / 2)) 1rem;
				pointer-events: none;
			}

			.site-nav a,
			.site-nav nav {
				pointer-events: auto;
			}

			.site-name {
				display: inline-flex;
				align-items: center;
				gap: 0.7rem;
				color: var(--ink);
				font-family: "Fraunces", Georgia, serif;
				font-size: 1.05rem;
				font-weight: 800;
				letter-spacing: -0.01em;
				/* dark halo keeps the text legible against bright aurora */
				text-shadow: 0 1px 16px rgba(3, 9, 16, 0.95), 0 0 30px rgba(3, 9, 16, 0.7);
			}

			.site-mark {
				width: 2.1rem;
				height: 2.1rem;
				border: 1px solid var(--line-strong);
				border-radius: 50%;
				background: rgba(255, 255, 255, 0.08);
				box-shadow: 0 0 26px rgba(97, 242, 255, 0.32), inset 0 0 12px rgba(112, 255, 158, 0.2);
				object-fit: cover;
			}

			.nav-links {
				display: flex;
				gap: 1.5rem;
				color: var(--ink-muted);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.84rem;
				letter-spacing: 0.04em;
				text-transform: uppercase;
				text-shadow: 0 1px 16px rgba(3, 9, 16, 0.95), 0 0 30px rgba(3, 9, 16, 0.7);
			}

			.nav-links a {
				position: relative;
				transition: color 180ms ease, text-shadow 180ms ease;
			}

			.nav-links a[aria-current="page"] {
				color: var(--green);
			}

			.nav-links a[aria-current="page"]::after {
				position: absolute;
				right: 0;
				bottom: -0.5rem;
				left: 0;
				height: 2px;
				border-radius: 2px;
				background: linear-gradient(90deg, var(--green), var(--cyan));
				box-shadow: 0 0 12px rgba(112, 255, 158, 0.6);
				content: "";
			}

			.nav-links a:hover,
			.nav-links a:focus-visible {
				color: var(--green);
				text-shadow: 0 0 18px rgba(112, 255, 158, 0.55), 0 1px 16px rgba(3, 9, 16, 0.9);
			}

			main {
				position: relative;
				z-index: 1;
				width: 100%;
				margin: 0 auto;
			}

			.page-section {
				position: relative;
				isolation: isolate;
				width: min(var(--page), calc(100% - 2rem));
				margin: 0 auto;
				padding: 7rem 0;
			}

			.page-section::before {
				position: absolute;
				inset: 1rem -8vw auto;
				z-index: -1;
				height: 30rem;
				/* soft radial blooms, no hard edges */
				background:
					radial-gradient(50% 70% at 24% 28%, rgba(112, 255, 158, 0.13), transparent 70%),
					radial-gradient(52% 72% at 68% 36%, rgba(84, 137, 255, 0.12), transparent 72%);
				content: "";
				filter: blur(48px);
				opacity: 0.9;
				pointer-events: none;
			}

			.hero {
				position: relative;
				isolation: isolate;
				display: grid;
				align-content: center;
				width: min(var(--page), calc(100% - 2rem));
				min-height: calc(100vh - 68px);
				margin: 0 auto;
				padding: 7rem 0 9rem;
			}

			.hero::before {
				position: absolute;
				inset: -4rem -12vw auto -6vw;
				z-index: -1;
				height: 34rem;
				/* organic aurora bloom that fades to nothing, no rectangle */
				background:
					radial-gradient(58% 80% at 72% 32%, rgba(112, 255, 158, 0.22), transparent 70%),
					radial-gradient(56% 76% at 48% 28%, rgba(97, 242, 255, 0.17), transparent 72%),
					radial-gradient(52% 72% at 28% 44%, rgba(181, 114, 255, 0.18), transparent 72%);
				content: "";
				filter: blur(52px);
				opacity: 0.85;
				pointer-events: none;
			}

			.hero-content {
				position: relative;
				max-width: 800px;
				animation: reveal-up 720ms ease-out both;
			}

			.kicker,
			.project-label {
				margin: 0 0 1rem;
				color: var(--green);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.8rem;
				font-weight: 600;
				letter-spacing: 0;
				text-transform: uppercase;
			}

			.hero h1,
			.page-title {
				margin: 0;
				color: var(--ink);
				font-family: "Fraunces", Georgia, serif;
				font-size: 6rem;
				font-weight: 900;
				letter-spacing: -0.025em;
				line-height: 0.92;
				text-wrap: balance;
				/* faint aurora glow lifting the name off the night sky */
				text-shadow: 0 0 48px rgba(97, 242, 255, 0.18), 0 0 90px rgba(112, 255, 158, 0.12);
			}

			.blurb {
				max-width: 620px;
				margin: 1.5rem 0 0;
				color: var(--ink-muted);
				font-size: 1.35rem;
				line-height: 1.55;
			}

			.section-heading {
				margin: 0 0 1.5rem;
				color: var(--ink);
				font-family: "Fraunces", Georgia, serif;
				font-size: 3.8rem;
				font-weight: 900;
				letter-spacing: -0.02em;
				line-height: 1;
			}

			/* projects as editorial index rows, not a grid */
			.project-rows {
				display: flex;
				flex-direction: column;
			}

			.project-row {
				position: relative;
				display: grid;
				grid-template-columns: 6rem minmax(0, 1fr);
				gap: 1.8rem;
				align-items: baseline;
				padding: 2.6rem 1.25rem 2.6rem 0.75rem;
				border-top: 1px solid var(--line);
				isolation: isolate;
				transition: padding-left 240ms ease;
			}

			.project-row:last-child {
				border-bottom: 1px solid var(--line);
			}

			/* aurora glow sweeps in from the left on hover */
			.project-row::before {
				position: absolute;
				inset: 0;
				z-index: -1;
				background: linear-gradient(
					90deg,
					rgba(112, 255, 158, 0.09),
					rgba(97, 242, 255, 0.05) 38%,
					transparent 68%
				);
				content: "";
				opacity: 0;
				transition: opacity 240ms ease;
			}

			.project-row:hover::before,
			.project-row:focus-within::before {
				opacity: 1;
			}

			.project-row:hover,
			.project-row:focus-within {
				padding-left: 1.6rem;
			}

			/* ghost-outline index number that fills with aurora on hover */
			.row-index {
				font-family: "Fraunces", Georgia, serif;
				font-size: 3.4rem;
				font-weight: 900;
				letter-spacing: -0.02em;
				line-height: 1;
				color: rgba(132, 152, 154, 0.5);
				transition:
					color 240ms ease,
					-webkit-text-stroke-color 240ms ease;
			}

			@supports (-webkit-text-stroke: 1px black) {
				.row-index {
					color: transparent;
					-webkit-text-stroke: 1px var(--ink-soft);
				}
			}

			.project-row:hover .row-index,
			.project-row:focus-within .row-index {
				color: var(--green);
				-webkit-text-stroke-color: transparent;
			}

			.row-kind {
				display: inline-block;
				margin: 0 0 0.55rem;
				color: var(--cyan);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.74rem;
				font-weight: 500;
				letter-spacing: 0.08em;
				text-transform: uppercase;
			}

			.row-title {
				margin: 0;
				font-family: "Fraunces", Georgia, serif;
				font-size: 2.3rem;
				font-weight: 800;
				letter-spacing: -0.015em;
				line-height: 1.04;
			}

			.row-link {
				color: var(--ink);
				transition: color 200ms ease;
			}

			.project-row:hover .row-link,
			.row-link:focus-visible {
				color: var(--green);
			}

			.row-arrow {
				display: inline-block;
				margin-left: 0.5rem;
				color: var(--green);
				font-size: 0.62em;
				opacity: 0;
				transform: translate(-5px, 5px);
				transition:
					opacity 200ms ease,
					transform 200ms ease;
			}

			.project-row:hover .row-arrow,
			.row-link:focus-visible .row-arrow {
				opacity: 1;
				transform: translate(0, 0);
			}

			.row-desc {
				max-width: 62ch;
				margin: 0.9rem 0 0;
				color: var(--ink-muted);
				font-size: 1.02rem;
				line-height: 1.6;
			}

			.link-row a:hover,
			.link-row a:focus-visible,
			.back-link:hover,
			.back-link:focus-visible {
				text-decoration: underline;
				text-underline-offset: 0.3em;
			}

			.project-tags {
				display: flex;
				flex-wrap: wrap;
				gap: 0.45rem;
				list-style: none;
				margin: 1.3rem 0 0;
				padding: 0;
			}

			.project-tags li {
				border: 1px solid rgba(112, 255, 158, 0.24);
				border-radius: 999px;
				background: rgba(112, 255, 158, 0.05);
				color: var(--ink-muted);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.72rem;
				letter-spacing: 0.02em;
				line-height: 1;
				padding: 0.4rem 0.72rem;
				white-space: nowrap;
			}

			.detail-tags {
				margin-top: 2rem;
			}

			.link-row {
				display: flex;
				flex-wrap: wrap;
				gap: 1rem;
				margin-top: 1.5rem;
			}

			.link-row a,
			.back-link {
				color: var(--green);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.9rem;
				font-weight: 700;
			}

			.back-link {
				display: inline-block;
				margin-bottom: 2rem;
			}

			.prose {
				max-width: 760px;
				margin-top: 2rem;
			}

			.prose p {
				margin: 0 0 1.25rem;
				color: var(--ink-muted);
				font-size: 1.08rem;
				line-height: 1.65;
			}

			.project-detail {
				min-height: calc(100vh - 58px);
			}

			.detail-header {
				max-width: 860px;
			}

			.detail-body {
				display: grid;
				grid-template-columns: minmax(0, 760px) minmax(12rem, 1fr);
				gap: 3rem;
				align-items: start;
				margin-top: 2.8rem;
			}

			.detail-aside {
				position: sticky;
				top: 7rem;
				border-left: 1px solid var(--line);
				padding-left: 1.25rem;
			}

			.detail-aside p {
				margin: 0 0 0.75rem;
				color: var(--ink-soft);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.82rem;
				line-height: 1.5;
				text-transform: uppercase;
			}

			.project-embed {
				width: 100%;
				min-height: 420px;
				margin-top: 2rem;
				border: 1px solid var(--line);
				border-radius: var(--radius);
				background: rgba(255, 255, 255, 0.06);
			}

			.text-page {
				min-height: calc(100vh - 58px);
			}

			.about-layout {
				display: grid;
				grid-template-columns: minmax(18rem, 0.8fr) minmax(0, 1.2fr);
				gap: 3rem;
				align-items: start;
				margin-top: 2.6rem;
			}

			.about-photo {
				position: relative;
				margin: 0;
				animation: reveal-up 740ms ease-out both;
			}

			.about-photo::before {
				position: absolute;
				inset: -1px;
				z-index: -1;
				border-radius: var(--radius);
				background: linear-gradient(135deg, var(--green), var(--cyan), var(--violet));
				content: "";
				filter: blur(18px);
				opacity: 0.42;
			}

			.about-photo img {
				display: block;
				width: 100%;
				aspect-ratio: 4 / 5;
				border: 1px solid var(--line-strong);
				border-radius: var(--radius);
				box-shadow: var(--shadow);
				object-fit: cover;
				object-position: 50% 38%;
			}

			.about-photo figcaption {
				margin-top: 0.7rem;
				color: var(--ink-soft);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.8rem;
			}

			.about-side {
				display: grid;
				gap: 1.7rem;
			}

			/* field-notes style fact list */
			.about-facts {
				display: grid;
				gap: 1rem;
				margin: 0;
			}

			.about-facts > div {
				display: grid;
				gap: 0.25rem;
				border-left: 1px solid var(--line);
				padding-left: 0.9rem;
			}

			.about-facts dt {
				color: var(--cyan);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.7rem;
				letter-spacing: 0.08em;
				text-transform: uppercase;
			}

			.about-facts dd {
				margin: 0;
				color: var(--ink-muted);
				font-size: 0.95rem;
				line-height: 1.45;
			}

			.site-footer {
				position: relative;
				z-index: 1;
				margin-top: 5rem;
				border-top: 1px solid var(--line);
				background: linear-gradient(180deg, transparent, rgba(2, 6, 10, 0.5));
			}

			.footer-inner {
				display: flex;
				flex-wrap: wrap;
				align-items: center;
				justify-content: space-between;
				gap: 1rem;
				width: min(var(--page), calc(100% - 2rem));
				margin: 0 auto;
				padding: 2rem 0 2.6rem;
			}

			.footer-name {
				color: var(--ink);
				font-family: "Fraunces", Georgia, serif;
				font-size: 1.05rem;
				font-weight: 800;
				letter-spacing: -0.01em;
			}

			.footer-links {
				display: flex;
				gap: 1.4rem;
			}

			.footer-links a,
			.footer-meta {
				color: var(--ink-soft);
				font-family: "IBM Plex Mono", monospace;
				font-size: 0.82rem;
			}

			.footer-links a {
				transition: color 180ms ease;
			}

			.footer-links a:hover,
			.footer-links a:focus-visible {
				color: var(--green);
			}

			/* scroll reveal: only hides when JS marks the page ready, so no-JS stays visible */
			.reveal-ready [data-reveal] {
				opacity: 0;
				transform: translateY(24px);
			}

			.reveal-ready [data-reveal].in-view {
				opacity: 1;
				transform: translateY(0);
				transition:
					opacity 720ms cubic-bezier(0.2, 0.65, 0.3, 1),
					transform 720ms cubic-bezier(0.2, 0.65, 0.3, 1);
				transition-delay: calc(var(--card-index, 0) * 80ms);
			}

			@keyframes aurora-drift {
				from {
					background-position: 0% 0%, 100% 0%;
					transform: skewX(-8deg) translate3d(-2vw, 0, 0);
				}

				to {
					background-position: 32% 0%, 68% 0%;
					transform: skewX(-4deg) translate3d(3vw, 1.5vh, 0);
				}
			}

			@keyframes twinkle {
				from {
					opacity: 0.58;
				}

				to {
					opacity: 0.95;
				}
			}

			@keyframes reveal-up {
				from {
					opacity: 0;
					transform: translateY(18px);
				}

				to {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@media (max-width: 920px) {
				.hero h1,
				.page-title {
					font-size: 4.2rem;
				}

				.section-heading {
					font-size: 2.9rem;
				}

				.about-layout,
				.detail-body {
					grid-template-columns: 1fr;
				}

				.detail-aside {
					position: static;
					border-left: 0;
					border-top: 1px solid var(--line);
					padding: 1.25rem 0 0;
				}
			}

			@media (max-width: 700px) {
				.site-nav {
					align-items: center;
					padding: 0.8rem 1rem;
				}

				.site-name span {
					display: none;
				}

				.nav-links {
					gap: 0.85rem;
					font-size: 0.75rem;
				}

				.hero {
					min-height: calc(100vh - 58px);
					padding: 5rem 0 6rem;
				}

				.hero h1,
				.page-title {
					font-size: 3.1rem;
					line-height: 1;
				}

				.blurb {
					font-size: 1.08rem;
				}

				.section-heading {
					font-size: 2.4rem;
				}

				.page-section {
					padding: 5rem 0;
				}

				.project-row {
					grid-template-columns: 1fr;
					gap: 0.5rem;
					padding: 2rem 0.5rem;
				}

				.project-row:hover,
				.project-row:focus-within {
					padding-left: 0.9rem;
				}

				.row-index {
					font-size: 2.4rem;
				}

				.row-title {
					font-size: 1.85rem;
				}

				.footer-inner {
					flex-direction: column;
					align-items: flex-start;
					gap: 0.7rem;
				}
			}

			@media (prefers-reduced-motion: reduce) {
				*,
				*::before,
				*::after {
					animation-duration: 1ms !important;
					animation-iteration-count: 1 !important;
					scroll-behavior: auto !important;
					transition-duration: 1ms !important;
				}
			}
		</style>
	</head>
	<body>
		<header class="site-nav">
			<a class="site-name" href={hrefWithBase("/")}>
				<img class="site-mark" src={penguinImage} alt="" />
				<span>Ronnie Wang</span>
			</a>
			<nav class="nav-links" aria-label="Main navigation">
				<a
					href={hrefWithBase("/projects")}
					aria-current={currentPath.startsWith("/projects") ? "page" : undefined}
					>Projects</a
				>
				<a
					href={hrefWithBase("/about")}
					aria-current={currentPath.startsWith("/about") ? "page" : undefined}
					>About</a
				>
			</nav>
		</header>
		<main>
			<slot />
		</main>
		<footer class="site-footer">
			<div class="footer-inner">
				<span class="footer-name">Ronnie Wang</span>
				<nav class="footer-links" aria-label="Footer">
					<a href="mailto:wronnie@seas.upenn.edu">Email</a>
					<a href="https://github.com/RonTonPeso">GitHub</a>
				</nav>
				<span class="footer-meta">© 2026 · Built with Astro</span>
			</div>
		</footer>
		<script is:inline>
			(function () {
				// reduced-motion users skip reveal entirely; content stays visible
				if (window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;
				document.documentElement.classList.add("reveal-ready");
				addEventListener("DOMContentLoaded", function () {
					var io = new IntersectionObserver(
						function (entries) {
							entries.forEach(function (entry) {
								if (entry.isIntersecting) {
									entry.target.classList.add("in-view");
									io.unobserve(entry.target);
								}
							});
						},
						{ rootMargin: "0px 0px -8% 0px", threshold: 0.05 }
					);
					document.querySelectorAll("[data-reveal]").forEach(function (el) {
						io.observe(el);
					});
				});
			})();
		</script>
	</body>
</html>
